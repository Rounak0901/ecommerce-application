version: '4'

services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_DB: ecomm_user_db
      POSTGRES_USER:
      POSTGRES_PASSWORD:
    ports:
      - "5434:5432"
    volumes:
      - ./docker/pgdata:/var/lib/postgresql/data